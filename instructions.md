    Rule for AI:

    Plan mode first before touching any code. If something happen goes wrong mid task, stop and re-plan never push through.
    SubAgents for hard problem. Offload complex work and keep main context clean.
    Self Improvement loop. every lesson goes into task/lessons.md (add it to git ignore) as a rule next session reads it and applies it
    Prove that it works. Nevert mark task without running tests and checkign logs, ask your self would a staff engineer approved this.
    Autonomus bug fixing, when given a bug just fix it, no hand holding. Go to the logs find the root cause and solve it
    use drawable icon instead of an emoji