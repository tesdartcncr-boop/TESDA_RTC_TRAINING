{
  "name": "trainer-portal",
  "version": "1.0.0",
  "description": "Trainer Portal - Full Stack Application",
  "scripts": {
    "dev": "concurrently \"cd backend && python -m uvicorn main:app --reload --host 0.0.0.0 --port 5000\" \"cd frontend_admin && npm run dev\" \"cd frontend_user && npm run dev\"",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --host 0.0.0.0 --port 5000",
    "dev:admin": "cd frontend_admin && npm run dev",
    "dev:user": "cd frontend_user && npm run dev",
    "install:all": "cd backend && pip install -r requirements.txt && cd ../frontend_admin && npm install && cd ../frontend_user && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "postgres": "^3.4.9"
  }
}
